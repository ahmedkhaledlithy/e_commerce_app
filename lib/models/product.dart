class Product {
  String name, description, price, category, image,productId;
  int quantity;

  Product({this.quantity,this.productId,this.name, this.description, this.price, this.category, this.image});
}
